# mkcd
Simple bash shell function to make a directory, and change into it.

# Installation
```bash
curl -s https://github.com/SidharthVJain/mkcd/install.sh | bash
```

# Usage
```bash
mkcd <dir_name>
```
